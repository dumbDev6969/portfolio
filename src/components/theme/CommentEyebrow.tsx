export default function CommentEyebrow({ children }: { children: React.ReactNode }) {
  return (
    <p className="font-mono text-sm text-[var(--text-comment)] mb-2">
      // {children}
    </p>
  );
}